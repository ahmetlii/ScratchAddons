export default async function ({ addon, global, console }) {
    if (!addon.tab.clientVersion) return;
    console.log("Canned texts are enabled.")
    const a = document.createElement("a");
    button.textContent = addon.settings.get("buttonName");
    link.appendChild(a);
    //<--!coming soon-->
}
  